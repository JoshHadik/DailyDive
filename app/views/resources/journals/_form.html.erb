<%= form_with(model: journal, local: true) do |form| %>
  <% if journal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(journal.errors.count, "error") %> prohibited this journal from being saved:</h2>

      <ul>
      <% journal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :title, autofill: "name", placeholder: "Name", autofocus: true %>
  </div>

  <div class="field">
    <%= form.text_field :caption, placeholder: "Handle" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
